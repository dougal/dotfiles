# TIL 1Password

## The `op` command line app does not appear to be able to find the main application.

1. Check the CLI is enabled in Settings -> Developer -> Enable CLI integration.
2. Check the terminal has `Full Disk Access` in Mac OS Settings.
